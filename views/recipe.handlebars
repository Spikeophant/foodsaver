<div class="row">
  <div class="col-auto">
    <h2>{{recipe.name}}</h2>
  </div>
</div>

<div class="row mt-4">
  <div class="col-md-6">
    <h3>Ingredients needed</h3>
  <div class="ingredients">
    <p>{{recipe.body}}</p>
  </div>
  </div>
</div>

<div class="flex justify-center m-3">
  <h2 class="block text-4xl text-emerald-700 font-semibold">{{recipe.name}}</h2>
</div>
<div class="grid grid-cols-3 m-5">
  <div class="col-span-1 flex justify-center">
    <img src="public/images/test.jpg" alt="tomatoes, carrots, zuchini in a bowl with rice">
  </div>
  <div>
    <h3 class="text-2xl col-span-2 flex justify-center">Ingredients and Instructions</h3>
    <p class="col-span-2 flex justify-center">{{recipe.body}}</p>
  </div>
</div>